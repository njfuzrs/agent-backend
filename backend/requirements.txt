# 这里只写范围。真正安装的版本在 requirements.lock，CI 和发版都装那一份，
# 不装这一份——范围约束会让 CI、本地、生产各装到不同版本（FastAPI 0.137 改了
# 路由树的形状，访问日志的 route 因此在 CI 上全变成 "-"）。
# 改了下面的范围之后重生成锁：
#   uv pip compile requirements.txt --python-version 3.10 --universal -o requirements.lock
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
oss2>=2.18.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
httpx>=0.27.0  # Starlette TestClient / identity 门禁依赖，不是评分器
alembic>=1.13.0
